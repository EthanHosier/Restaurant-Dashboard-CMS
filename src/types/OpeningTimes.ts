import OpenClose from "./OpenClose";

export default interface OpeningTimes {
  monday: OpenClose;
  tuesday: OpenClose;
  wednesday: OpenClose;
  thursday: OpenClose;
  friday: OpenClose;
  saturday: OpenClose;
  sunday: OpenClose;
}
